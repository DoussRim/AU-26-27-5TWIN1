[project]
name = "5twin1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "DoussRim", email = "rim.douss@esprit.tn" }
]
requires-python = ">=3.11"
dependencies = [
    "django==5.2",
    "pillow>=12.3.0",
]

[project.scripts]
5twin1 = "5twin1:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
